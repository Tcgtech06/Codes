# 🏗️ KnitInfo Architecture Overview

## System Architecture (Simple View)

```
┌─────────────────────────────────────────────────────────────┐
│                     YOUR COMPUTER                            │
│                                                              │
│  ┌────────────────────┐         ┌────────────────────┐     │
│  │   Frontend (React) │         │   Backend (Go)     │     │
│  │                    │         │                    │     │
│  │  - Needs: npm ✓    │◄───────►│  - Needs: Go ✓     │     │
│  │  - Port: 3000      │   API   │  - Port: 8080      │     │
│  │  - Status: Later   │  Calls  │  - Status: Ready   │     │
│  └────────────────────┘         └──────────┬─────────┘     │
│                                             │                │
└─────────────────────────────────────────────┼───────────────┘
                                              │
                                              │ Database
                                              │ Connection
                                              ▼
                                   ┌──────────────────┐
                                   │   SUPABASE       │
                                   │   (Cloud)        │
                                   │                  │
                                   │  - Database ✓    │
                                   │  - Tables ✓      │
                                   │  - API Keys ✓    │
                                   └──────────────────┘
```

---

## What You're Working On NOW

```
┌─────────────────────────────────────────────────────────────┐
│                    CURRENT FOCUS                             │
│                                                              │
│              ┌────────────────────────┐                      │
│              │   Backend (Go)         │                      │
│              │                        │                      │
│              │  1. Create .env ⏳     │                      │
│              │  2. Add credentials ⏳ │                      │
│              │  3. Test server ⏳     │                      │
│              └───────────┬────────────┘                      │
│                          │                                   │
│                          │ Connects to                       │
│                          ▼                                   │
│              ┌────────────────────────┐                      │
│              │   Supabase ✓           │                      │
│              │   (Already Setup)      │                      │
│              └────────────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

---

## File Structure Explained

```
KnitInfo/
│
├── KnitInfo_Backend/              ← YOU ARE HERE
│   │
│   ├── cmd/
│   │   └── server/
│   │       └── main.go            ← Main application file
│   │
│   ├── pkg/
│   │   ├── database/
│   │   │   └── supabase.go        ← Database connection
│   │   ├── models/
│   │   │   └── models.go          ← Data structures
│   │   └── repository/
│   │       ├── company.go         ← Company operations
│   │       ├── excel_upload.go    ← Excel handling
│   │       └── ...                ← Other operations
│   │
│   ├── migrations/
│   │   └── 001_initial_schema.sql ← Database schema (✓ Done)
│   │
│   ├── .env.example               ← Template (✓ Created)
│   ├── .env                       ← Your secrets (⏳ Create this)
│   ├── go.mod                     ← Go dependencies
│   ├── go.sum                     ← Dependency checksums
│   │
│   ├── BACKEND_SETUP_GUIDE.md     ← Complete guide (✓ Created)
│   ├── QUICK_REFERENCE.md         ← Quick help (✓ Created)
│   └── check_backend.bat          ← Validation script (✓ Created)
│
└── KnitInfo_Frontend/             ← For later (needs npm)
    └── ...
```

---

## Data Flow: Excel Upload Feature

```
┌──────────────┐
│   User       │
│  (You/Admin) │
└──────┬───────┘
       │
       │ 1. Upload Excel File
       ▼
┌──────────────────────────────────────┐
│   Frontend (React)                   │
│   - File selection UI                │
│   - Upload button                    │
└──────┬───────────────────────────────┘
       │
       │ 2. HTTP POST Request
       │    /api/v1/excel/parse
       ▼
┌──────────────────────────────────────┐
│   Backend (Go)                       │
│   ┌────────────────────────────┐    │
│   │ parseExcelFile() function  │    │
│   │                            │    │
│   │ 1. Receive file            │    │
│   │ 2. Parse Excel             │    │
│   │ 3. Extract data:           │    │
│   │    - Company Name          │    │
│   │    - Address               │    │
│   │    - Phone                 │    │
│   │    - Email                 │    │
│   │    - Products              │    │
│   │ 4. Validate data           │    │
│   │ 5. Return JSON             │    │
│   └────────────────────────────┘    │
└──────┬───────────────────────────────┘
       │
       │ 3. JSON Response
       ▼
┌──────────────────────────────────────┐
│   Frontend (React)                   │
│   - Display companies in cards       │
│   - Phone → Dial pad                 │
│   - Email → Email app                │
│   - Show all products                │
└──────────────────────────────────────┘
```

---

## Excel File Structure (Fixed Format)

```
┌─────────────────────────────────────────────────────────────┐
│  Row 1 (Headers - Ignored by backend)                       │
├────────┬──────────┬─────────┬───────┬────────┬─────────────┤
│ Serial │ Company  │ Address │ Phone │ Email  │ Products    │
│ Number │ Name     │         │       │        │             │
├────────┼──────────┼─────────┼───────┼────────┼─────────────┤
│   1    │ ABC Ltd  │ 123 St  │ +91.. │ a@b.c  │ Yarn, Fab   │ ← Row 2
│   2    │ XYZ Inc  │ 456 Rd  │ +91.. │ x@y.z  │ Cotton      │ ← Row 3
│  ...   │  ...     │  ...    │  ...  │  ...   │  ...        │
└────────┴──────────┴─────────┴───────┴────────┴─────────────┘
         ↑          ↑         ↑       ↑        ↑
      Ignored   Required  Required Required Required
```

**Backend extracts:**
- Column 2: Company Name (Required)
- Column 3: Address
- Column 4: Phone Number
- Column 5: Email
- Column 6: Products

---

## Environment Variables Explained

```
.env File Structure:
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  PORT=8080                    ← Server runs on this port    │
│                                                              │
│  SUPABASE_DB_URL=postgres://  ← Database connection string  │
│    ↑                                                         │
│    └─ Backend uses this to connect to database              │
│                                                              │
│  SUPABASE_URL=https://...     ← Supabase project URL        │
│  SUPABASE_ANON_KEY=eyJ...     ← Public API key              │
│  SUPABASE_SERVICE_KEY=eyJ...  ← Admin API key               │
│    ↑                                                         │
│    └─ For Supabase API calls (if needed)                    │
│                                                              │
│  JWT_SECRET=knitinfo-...      ← Signs authentication tokens │
│    ↑                                                         │
│    └─ Keeps your login sessions secure                      │
│                                                              │
│  ADMIN_USERNAME=admin         ← Login username              │
│  ADMIN_PASSWORD=KnitInfo...   ← Login password              │
│    ↑                                                         │
│    └─ Credentials to access admin features                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Go)                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Language:        Go 1.21+                                   │
│  Web Framework:   Echo v4                                    │
│  Database:        PostgreSQL (via Supabase)                  │
│  ORM:             sqlx                                       │
│  Excel Parser:    excelize v2                                │
│  Auth:            JWT (golang-jwt)                           │
│  Environment:     godotenv                                   │
│                                                              │
│  ✓ No npm needed                                             │
│  ✓ No Node.js needed                                         │
│  ✓ Pure Go application                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   FRONTEND (React)                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Language:        TypeScript/JavaScript                      │
│  Framework:       React + Next.js                            │
│  Runtime:         Node.js                                    │
│  Package Manager: npm                                        │
│                                                              │
│  ⏳ Setup later                                              │
│  ✓ Needs npm                                                 │
│  ✓ Needs Node.js                                             │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## API Endpoints (Backend)

```
Public Endpoints (No login required):
├── GET  /health                          ← Check if server is running
├── POST /api/v1/auth/login               ← Login to get token
├── GET  /api/v1/companies                ← List all companies
├── GET  /api/v1/categories               ← List categories
└── ...

Protected Endpoints (Login required):
├── POST /api/v1/excel/parse              ← Parse Excel file ⭐
├── POST /api/v1/companies                ← Add company
├── PUT  /api/v1/companies/:id            ← Update company
├── DELETE /api/v1/companies/:id          ← Delete company
└── ...

Excel Upload Endpoint (Your focus):
POST /api/v1/excel/parse
├── Requires: JWT token (login first)
├── Accepts: Excel file (.xlsx)
├── Returns: JSON with parsed company data
└── Used by: Frontend to display companies
```

---

## Current Status Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│                    PROJECT STATUS                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅ Supabase Project Created                                 │
│  ✅ Database Schema Created                                  │
│  ✅ Tables Exist in Database                                 │
│  ✅ Backend Code Exists                                      │
│  ✅ Go Dependencies Downloaded                               │
│  ✅ Code Compiles Successfully                               │
│  ✅ Excel Parser Implemented                                 │
│                                                              │
│  ⏳ .env File (In Progress)                                  │
│  ⏳ Backend Server Testing (Next)                            │
│  ⏳ Frontend Setup (Later)                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## What Happens When You Run the Server

```
Step 1: Start Server
$ go run cmd/server/main.go

Step 2: Server Initialization
┌─────────────────────────────────────┐
│ 1. Load .env file                   │
│ 2. Connect to Supabase database     │
│ 3. Initialize repositories          │
│ 4. Setup API routes                 │
│ 5. Start listening on port 8080     │
└─────────────────────────────────────┘

Step 3: Server Ready
✓ Server running on http://localhost:8080
✓ Ready to accept requests
✓ Excel parsing endpoint available

Step 4: Test
Open browser → http://localhost:8080/health
Response: {"status":"ok","service":"KnitInfo API"}
```

---

## Summary: What You Need to Know

```
┌─────────────────────────────────────────────────────────────┐
│  KEY POINTS                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Backend = Go (not Node.js)                               │
│     → No npm needed for backend                              │
│                                                              │
│  2. .env = Configuration file                                │
│     → Create manually with your Supabase credentials         │
│     → Not auto-generated (security)                          │
│                                                              │
│  3. Excel parsing is ready                                   │
│     → Already implemented in backend                         │
│     → Just needs server to run                               │
│                                                              │
│  4. Validation without running                               │
│     → Use check_backend.bat                                  │
│     → Checks everything except database connection           │
│                                                              │
│  5. npm is for frontend only                                 │
│     → Install later when working on React                    │
│     → Not needed now                                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Your Next Action

```
┌─────────────────────────────────────────────────────────────┐
│                    IMMEDIATE NEXT STEPS                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Open Supabase Dashboard                                  │
│     → Get your credentials                                   │
│                                                              │
│  2. Copy .env.example to .env                                │
│     → Fill in your Supabase credentials                      │
│                                                              │
│  3. Run check_backend.bat                                    │
│     → Verify everything is ready                             │
│                                                              │
│  4. Start the server                                         │
│     → go run cmd/server/main.go                              │
│                                                              │
│  5. Test the health endpoint                                 │
│     → http://localhost:8080/health                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

Good luck! 🚀
